@model AgendamentosPD.Models.Sala

@{
    ViewBag.Title = "Delete";
}

<div class="main-w">
    <div class="row p-2">
        <div class="col-md-12 bg-white">
            <h2 class="text-success py-2">Excluir</h2>

            <h3 class="text-danger">Tem certeza que quer excluir?</h3>
            <div>
                <h4>Sala</h4>
                <hr />
                <dl class="dl-horizontal">
                    <dt>
                        @Html.DisplayNameFor(model => model.numero)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.numero)
                    </dd>

                    <dt>
                        @Html.DisplayNameFor(model => model.bloco)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.bloco)
                    </dd>

                </dl>

                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <div class="form-actions no-color py-2">
                        <input type="submit" value="Excluir" class="btn btn-danger" />
                        <br><br><br>
                        @Html.ActionLink("Voltar para lista", "Index")
                    </div>
                }
            </div>
        </div>
    </div>
</div>

